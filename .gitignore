# 本地 HTTPS 自签证书 —— key.pem 是私钥，绝不能进仓库
cert.pem
key.pem

# 编辑器 / 系统临时文件
.!*!*
.DS_Store
.vscode/
node_modules/

# Python
.venv/
__pycache__/
*.pyc
rdk/frames/

# 声网 App 证书 —— 密钥，只能放服务器，绝不进仓库、绝不进网页
agora.env

# 电脑版本机身份（好友码 + 名字）
.byside-id
